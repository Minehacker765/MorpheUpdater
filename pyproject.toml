[project]
name = "morpheupdater"
version = "0.1.0"
description = "Async daemon that patches Play APKs with Morphe and publishes an F-Droid repo"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.14.3",
    "aiofiles>=24.1.0",
    "pillow>=12.3.0",
]
